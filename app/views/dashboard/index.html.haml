- if @has_authorized_projects
  .projects
    .activities.span8
      = render 'activities'
    .side.span4
      = render 'sidebar'

- else
  = render "zero_authorized_projects"

- content_for :script_on_load do
  :plain
    Pager.init(20);
